<% include header.html %>

  <div class="container_pai_produtos">

      <% for (let index=0; index < cadprod.length ; index++) { %>
                
                  <div class="container_filho_produtos">

                      <div class="card-foto">
                        <a href="/detalhes"><img class="foto_produto" src="/images/produtos/<%= cadprod[index].codigo %>.jpg"></a>
                      </div>

                      <div class="card-texto">
                        <div> <%= cadprod[index].descricao %> </div>
                        <div><%= cadprod[index].marca %></div>
                        <div><%= cadprod[index].modelo %></div>
                        <div>Preço: <%= cadprod[index].preco %></div>
                      </div>  
                  </div>

        <% } %>

    </div>

<% include footer.html %>